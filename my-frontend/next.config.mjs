/** @type {import('next').NextConfig} */

const nextConfig = {
    basePath: "",
    assetPrefix: "/",
    trailingSlash: true,
    reactStrictMode: true,
    output: "export",
};

export default nextConfig;
